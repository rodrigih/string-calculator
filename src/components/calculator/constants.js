export const OPERATIONS = {
  add: (acc, curr) => acc + curr,
  subtract: (acc, curr) => acc - curr,
  multiply: (acc, curr) => acc * curr,
  divide: (acc, curr) => acc / curr
};

export const JOIN_CHARS = {
  add: " + ",
  subtract: " - ",
  multiply: " * ",
  divide: " / "
};
